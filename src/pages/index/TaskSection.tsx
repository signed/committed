import { Task } from '../../core/ReleaseTasks'

export type TaskSectionProperties = {
  tasks: Task[]
}
export const TaskSection = (_props: TaskSectionProperties) => {
  return <div>Implement them tasks</div>
}
